
Vec1 is { x: int }
Vec2 is { x: int, y: int }
Vec3 is { x: int, y: int, z: int }

Position is enum {
    Pos0,
    Pos1: Vec1,
    Pos2: Vec2,
    Pos3: Vec3,
}

fn test() {
}

fn main() {
    test_list := list_new();
    test_list.add(10)
    test_list.add(20)
    test_list.add(30)
    test_list.add(40)

    test_list.get(1)
}
