
Pos :: { x: float, y: float }
Trans :: { pos: Pos }

main := fn() {
    trans : Trans = { pos: { x: 0., y: 10., z: 10. } };

    result := if trans.pos.x { "true" } else { "false" }

    print(result)
}

